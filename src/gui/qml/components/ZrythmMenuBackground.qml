import QtQuick
import QtQuick.Controls.Basic

Rectangle {

    // required property ZrythmMenu menu

    // implicitWidth: menu.contentItem.childrenRect.width
    // implicitHeight: menu.contentHeight
    color: palette.window
    border.color: palette.mid
    border.width: 1
}
