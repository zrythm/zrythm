# We are building some programs to be installed in bin dir
# There is only one program to build, zrythm
bin_PROGRAMS = zrythm

AM_CXXFLAGS = $(GTK_CFLAGS)

# To create zrythm, compile main.cpp
zrythm_SOURCES = main.cpp
zrythm_LDADD = $(GTK_LIBS)
